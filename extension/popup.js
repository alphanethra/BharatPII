document.getElementById("yesBtn")
.addEventListener("click", () => {

    alert(
        "Redaction will be handled automatically during upload."
    );
});

document.getElementById("noBtn")
.addEventListener("click", () => {

    window.close();
});